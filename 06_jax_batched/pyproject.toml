[project]
name = "06-jax-batched"
version = "0.1.0"
description = "microGPT in JAX with batched training via vmap"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jax>=0.6.0",
]
