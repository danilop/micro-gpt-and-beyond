# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Tool caches
# ruff writes .ruff_cache/.gitignore containing "*", so this is belt and
# braces: it keeps the directory ignored even if that inner file is lost.
.ruff_cache/

# macOS
.DS_Store

# Built tutorial bundle: wtc-build regenerates it, and the deploy workflow
# rebuilds it before copying, so the committed copy was never read.
walk-the-code/data/units.json
