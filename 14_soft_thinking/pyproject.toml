[project]
name = "14-soft-thinking"
version = "0.1.0"
description = "microGPT with soft thinking (concept tokens) in PyTorch"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.10.0",
]
