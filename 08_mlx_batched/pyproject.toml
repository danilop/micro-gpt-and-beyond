[project]
name = "08-mlx-batched"
version = "0.1.0"
description = "microGPT in MLX with batched training on Apple Silicon"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mlx>=0.30.6",
]
