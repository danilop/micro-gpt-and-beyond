[project]
name = "11-speculative-decoding"
version = "0.1.0"
description = "microGPT with speculative decoding in PyTorch"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.10.0",
]
