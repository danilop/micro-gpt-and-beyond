{
  "title": "microGPT and Beyond",
  "tagline": "Build a GPT from scratch, one concept at a time, from pure Python to production serving.",
  "repo_url": "https://github.com/danilop/micro-gpt-and-beyond",
  "code_dir": "../labs",
  "language": "python",
  "chapters": [
    {
      "id": "foundations",
      "title": "Foundations",
      "description": "<p>Every deep learning framework hides the same core loop: <strong>forward pass → loss → backward pass → update</strong>. These two labs strip away all abstraction so you see exactly what happens.</p><p>Lab 01 implements Karpathy's original microGPT — a complete GPT in pure Python with a hand-built scalar autograd engine. Every gradient flows through a graph you can inspect. Lab 02 replaces scalars with NumPy arrays for speed, but still derives every gradient by hand. Together they answer: <em>what is a neural network actually computing?</em></p><p>Start here if you're new to transformers, or if you want to truly understand what <code>loss.backward()</code> does under the hood.</p>",
      "diagram": "graph LR\n  A[Input] --> B[Forward Pass]\n  B --> C[Loss]\n  C --> D[Backward Pass]\n  D --> E[Update Weights]\n  E --> A",
      "comparison_diagram": "compare_foundations",
      "labs": ["01_pure_python", "02_numpy_manual_backprop"]
    },
    {
      "id": "frameworks",
      "title": "Frameworks",
      "description": "<p>The same transformer architecture expressed in three different frameworks — <strong>PyTorch, JAX, and MLX</strong> — each with a single-example and batched variant.</p><p>PyTorch is imperative and object-oriented: you define <code>nn.Module</code> classes and call <code>.backward()</code>. JAX is purely functional: no classes, no mutation, explicit random keys, and <code>jax.vmap</code> to auto-vectorize. MLX targets Apple Silicon with unified CPU/GPU memory and lazy evaluation.</p><p>Comparing these side-by-side reveals what's essential to the algorithm vs. what's just framework convention. The batched variants add padding, masking, and mini-batch training — the bridge from educational toy to real training.</p>",
      "diagram": "graph TD\n  A[Same Transformer] --> B[PyTorch: nn.Module + .backward]\n  A --> C[JAX: Pure Functions + jax.grad]\n  A --> D[MLX: Lazy Eval + Apple GPU]\n  B --> E[Compare Side-by-Side]\n  C --> E\n  D --> E",
      "comparison_diagram": "compare_frameworks",
      "labs": ["03_pytorch", "04_pytorch_batched", "05_jax", "06_jax_batched", "07_mlx", "08_mlx_batched"]
    },
    {
      "id": "tokenization-and-architecture",
      "title": "Tokenization & Architecture",
      "description": "<p>Modern LLMs differ from the original transformer in three key ways: <strong>how they tokenize text, encode position, and share attention heads</strong>. These labs implement each upgrade from scratch.</p><p>BPE tokenization (Lab 09) replaces character-level input with learned subword units — the same algorithm behind GPT's tokenizer. RoPE (Lab 10) encodes position by rotating query and key vectors in complex space, enabling length generalization. GQA (Lab 11) shares key-value heads across query heads, cutting memory without losing quality.</p><p>These are the building blocks of LLaMA, Mistral, and every modern open-weight model.</p>",
      "diagram": "graph LR\n  A[Text] --> B[BPE Tokenizer]\n  B --> C[Embeddings]\n  C --> D[RoPE Position]\n  D --> E[GQA Attention]",
      "labs": ["09_bpe_tokenizer", "10_rope", "11_gqa"]
    },
    {
      "id": "inference-optimization",
      "title": "Inference Optimization",
      "description": "<p>Autoregressive generation is inherently sequential: each token depends on all previous tokens. These labs tackle the two fundamental questions: <strong>how to avoid redundant computation</strong> and <strong>how to control output quality</strong>.</p><p>The KV cache (Lab 12) is the single most important inference optimization — it stores previously computed key-value pairs so each new token only needs one matrix multiply instead of reprocessing the entire sequence. Sampling strategies (Lab 13) show how temperature, top-k, top-p, and min-p produce completely different outputs from the same model.</p><p>Every production LLM serving system uses both of these techniques.</p>",
      "diagram": "graph LR\n  A[Model] --> B[KV Cache]\n  B --> C[Next Token Logits]\n  C --> D[Sampling Strategy]\n  D --> E[Output Token]\n  E --> B",
      "labs": ["12_kv_cache", "13_sampling"]
    },
    {
      "id": "fine-tuning-and-deployment",
      "title": "Fine-tuning & Deployment",
      "description": "<p>Training a model from scratch is expensive. These labs show how to <strong>adapt and compress</strong> existing models efficiently.</p><p>LoRA (Lab 14) freezes the original model weights and injects tiny low-rank matrices that capture task-specific knowledge — training only ~1% of parameters while matching full fine-tuning quality. Quantization (Lab 16) compresses a trained model from 32-bit floats to 8-bit integers, reducing size by 4× and speeding up inference. This is how production models run on edge devices.</p><p>Together they answer: <em>how do you take a large pretrained model and make it yours, then make it fast?</em></p>",
      "diagram": "graph LR\n  A[Pretrained Model] --> B[LoRA Adapters]\n  B --> C[Fine-tuned Model]\n  C --> D[INT8 Quantization]\n  D --> E[Production Model]",
      "labs": ["14_lora", "15_pytorch_quantized"]
    },
    {
      "id": "alternative-paradigms",
      "title": "Alternative Paradigms",
      "description": "<p>Not all language models generate left-to-right. These labs explore <strong>fundamentally different approaches</strong> to text generation.</p><p>Text diffusion (Lab 16) starts from pure noise — all [MASK] tokens — and iteratively unmasks them, like image diffusion but for text. Soft thinking (Lab 17) preserves the full output distribution instead of collapsing to a single token, passing probability-weighted \"concept tokens\" forward. Soft training (Lab 18) closes the train-test gap by teaching the model to work with its own uncertain outputs during training.</p><p>These represent active research frontiers in how we think about language generation.</p>",
      "diagram": "graph TD\n  A[Text Generation] --> B[Autoregressive: Token by Token]\n  A --> C[Diffusion: Unmask from Noise]\n  A --> D[Soft Thinking: Blend Probabilities]\n  B --> E[Different Tradeoffs]\n  C --> E\n  D --> E",
      "labs": ["16_text_diffusion", "17_soft_thinking", "18_soft_training"]
    },
    {
      "id": "production-serving",
      "title": "Production Serving",
      "description": "<p>Serving LLMs at scale requires solving memory, latency, and throughput problems simultaneously. These labs implement the <strong>key algorithms behind vLLM, FlashAttention, and TensorRT-LLM</strong>.</p><p>Speculative decoding (Lab 19) uses a small draft model to guess ahead while a larger model verifies in one pass — lossless speedup. FlashAttention (Lab 20) restructures attention to stay in fast on-chip SRAM, avoiding the memory wall. PagedAttention (Lab 21) applies OS-style virtual memory paging to KV caches, eliminating fragmentation. Disaggregated serving (Lab 22) splits prefill and decode onto separate workers, removing head-of-line blocking.</p><p>These are the techniques that make it possible to serve millions of concurrent LLM requests.</p>",
      "diagram": "graph LR\n  A[Request] --> B[Prefill Worker]\n  B --> C[KV Cache Pages]\n  C --> D[Decode Worker]\n  D --> E[Speculative Draft]\n  E --> F[Verify & Output]",
      "labs": ["19_speculative_decoding", "20_tiled_attention", "21_paged_attention", "22_disaggregated_serving"]
    }
  ],
  "labs": [
    {"id": "01_pure_python", "file": "microgpt.py", "title": "microGPT, Pure Python", "tagline": "Karpathy's original microGPT, unmodified except for the dataset path. The complete algorithm in one file.", "description": "<p>This is where it all starts — Andrej Karpathy's <strong>microGPT</strong>, a complete GPT implementation in a single file of pure Python with zero dependencies.</p><p>The code builds a scalar autograd engine (the <code>Value</code> class), implements a character-level transformer with multi-head attention, RMSNorm, and a linear learning rate schedule, then trains it on ~32,000 human names. Every operation — every multiply, every addition — is tracked in a computation graph so gradients can flow backward automatically.</p><p>This is the most educational implementation: you can literally trace every number through the network. It's also the slowest, because Python loops over individual scalars. The next labs will show how frameworks eliminate that overhead while keeping the same algorithm.</p>", "run_command": ["python3", "microgpt.py"]},
    {"id": "02_numpy_manual_backprop", "file": "main.py", "title": "NumPy with Manual Backpropagation", "tagline": "Same architecture, but NumPy handles the math. Every gradient is derived by hand.", "description": "<p>This lab replaces Python scalars with <strong>NumPy arrays</strong>, making the forward pass fast — but keeps every gradient derived by hand in the backward pass.</p><p>You'll see the chain rule applied explicitly to matrix multiplications, softmax, RMSNorm, and cross-entropy loss. Each backward function computes <code>dL/dX</code> given <code>dL/dY</code> from the layer above. This is exactly what PyTorch's autograd does automatically, but here you can read every line.</p><p>The key insight: the forward pass is just linear algebra, and the backward pass is just more linear algebra applied in reverse order. Once you see it done manually, automatic differentiation feels like a natural shortcut rather than magic.</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "03_pytorch", "file": "main.py", "title": "PyTorch", "tagline": "PyTorch handles tensors and gradients. See how much boilerplate disappears.", "description": "<p>The same transformer architecture, now in <strong>PyTorch</strong>. Autograd handles all gradients automatically — no more manual backward passes.</p><p>The model is defined as <code>nn.Module</code> subclasses with learnable <code>nn.Parameter</code> tensors. Call <code>loss.backward()</code> and PyTorch traces the computation graph, computes all gradients, and stores them in <code>.grad</code> attributes. Compare this to Lab 02 to see exactly how much boilerplate disappears.</p><p>This is the imperative, object-oriented style that most ML practitioners use daily. The next lab adds batching to bridge the gap to real training.</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "04_pytorch_batched", "file": "main.py", "title": "PyTorch Batched", "tagline": "Mini-batch training bridges the gap between educational toy and real training.", "description": "<p>Real training doesn't process one example at a time — it uses <strong>mini-batches</strong>. This lab adds padding, attention masking, and batch-level loss computation to the PyTorch model.</p><p>Names have different lengths, so shorter ones are padded with a special token. The attention mask prevents the model from attending to padding positions. Loss is computed only on real tokens, not padding. These are the same techniques used in production training of billion-parameter models.</p><p>The batch size of 32 means 32× more work per gradient step, but the gradient is also 32× less noisy. This is the fundamental tradeoff of mini-batch SGD.</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "05_jax", "file": "main.py", "title": "JAX", "tagline": "Purely functional style. No classes, no hidden state, no mutation.", "description": "<p><strong>JAX</strong> takes a radically different approach: everything is a pure function. No classes, no mutable state, no <code>.backward()</code> method.</p><p>Model parameters are plain dictionaries of arrays. The forward pass is a function that takes parameters and data, returns a loss. <code>jax.grad</code> transforms that function into one that returns gradients. <code>jax.jit</code> compiles it for speed. Random numbers require explicit keys that must be split — no hidden global state.</p><p>This functional style makes it trivial to compose transformations: differentiate, vectorize, parallelize, compile — all by wrapping functions.</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "06_jax_batched", "file": "main.py", "title": "JAX Batched", "tagline": "Use jax.vmap to automatically vectorize single-example code across a batch.", "description": "<p><code>jax.vmap</code> is JAX's killer feature for batching: write code for a <strong>single example</strong>, and vmap automatically vectorizes it across a batch dimension.</p><p>Instead of manually reshaping tensors and adding batch dimensions everywhere (like in PyTorch batched), you write the clean single-example version and let the compiler figure out the batching. The result is identical to hand-batched code but with zero manual effort.</p><p>This lab shows the same padding and masking as PyTorch batched, but the core model code stays single-example clean.</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "07_mlx", "file": "main.py", "title": "MLX (Apple Silicon)", "tagline": "Running on Apple Silicon GPU via MLX. Unified memory, lazy evaluation.", "description": "<p><strong>MLX</strong> is Apple's framework for machine learning on Apple Silicon. It uses unified memory — the same physical RAM is shared between CPU and GPU, so there's no data transfer overhead.</p><p>The API looks like PyTorch (modules, parameters, optimizers), but computation is lazy: operations build a graph that's only evaluated when you call <code>mx.eval()</code>. This lets the framework fuse operations and optimize memory access patterns.</p><p>If you have a Mac with M1/M2/M3/M4, this lab runs on your GPU with zero configuration.</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "08_mlx_batched", "file": "main.py", "title": "MLX Batched", "tagline": "Mini-batch training on Apple Silicon, same padding and masking as PyTorch batched.", "description": "<p>The batched version of the MLX model, with the same <strong>padding, masking, and mini-batch training</strong> as PyTorch batched (Lab 04).</p><p>MLX's lazy evaluation shines here: the framework can see the entire batch computation graph before executing, enabling better memory planning and operation fusion on the Apple GPU.</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "09_bpe_tokenizer", "file": "main.py", "title": "BPE Tokenizer", "tagline": "Byte-Pair Encoding from scratch in pure Python, with zero dependencies.", "description": "<p><strong>Byte-Pair Encoding</strong> is the tokenization algorithm behind GPT, LLaMA, and most modern LLMs. This lab implements it from scratch in pure Python.</p><p>BPE starts with individual characters and iteratively merges the most frequent adjacent pair into a new token. After enough merges, common words become single tokens while rare words are split into subword pieces. This balances vocabulary size against sequence length.</p><p>The lab trains a BPE tokenizer on the names dataset and uses it with a simple bigram model. Understanding tokenization is essential — it's the first thing that happens to every input and the last thing that happens to every output.</p>", "run_command": ["python3", "main.py"]},
    {"id": "10_rope", "file": "main.py", "title": "Rotary Position Embeddings (RoPE)", "tagline": "Position encoded by rotating Q and K vectors in complex space.", "description": "<p><strong>RoPE</strong> encodes position information by rotating query and key vectors in complex space. Instead of adding a position embedding to the input, RoPE multiplies Q and K by rotation matrices whose angles depend on position.</p><p>The key property: the dot product between rotated Q and K depends only on their <em>relative</em> position, not absolute. This means the model naturally generalizes to longer sequences than it was trained on. RoPE is used by LLaMA, Mistral, Qwen, and most modern open-weight models.</p><p>The implementation uses the real-valued form (pairs of dimensions rotated together) rather than complex numbers, matching production implementations.</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "11_gqa", "file": "main.py", "title": "Grouped-Query Attention (GQA/MQA)", "tagline": "From MHA to MQA to GQA: sharing KV heads reduces memory.", "description": "<p><strong>Grouped-Query Attention</strong> is a simple but powerful optimization: instead of giving every query head its own key-value head, multiple query heads <em>share</em> the same KV head.</p><p>Standard multi-head attention (MHA) has equal numbers of Q, K, and V heads. Multi-query attention (MQA) uses just one KV head for all queries. GQA is the middle ground — groups of query heads share a KV head. This reduces KV cache memory (critical for inference) with minimal quality loss.</p><p>LLaMA 2 70B, Mistral, and Gemma all use GQA. This lab implements all three variants so you can compare them directly.</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "12_kv_cache", "file": "main.py", "title": "KV Cache", "tagline": "The single most important inference optimization.", "description": "<p>During autoregressive generation, each new token attends to all previous tokens. Without caching, this means recomputing K and V for the entire sequence at every step — O(n²) total work for n tokens.</p><p>The <strong>KV cache</strong> stores previously computed key and value tensors and appends new ones incrementally. Each generation step only computes K and V for the new token, then concatenates with the cache. This reduces per-step work from O(n) to O(1) for the projection, though attention itself is still O(n).</p><p>Every production LLM serving system uses KV caching. It's the difference between seconds and minutes for long sequences.</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "13_sampling", "file": "main.py", "title": "Sampling Strategies", "tagline": "Five strategies producing completely different output from the same model.", "description": "<p>The model outputs logits — raw scores for every token in the vocabulary. <strong>How you convert logits to a token choice</strong> dramatically affects output quality.</p><p>This lab implements five strategies: greedy (always pick the highest), temperature scaling (sharpen or flatten the distribution), top-k (only consider the k most likely), top-p/nucleus (only consider tokens whose cumulative probability reaches p), and min-p (only consider tokens above a fraction of the top token's probability).</p><p>The same model produces boring repetitive output with greedy sampling and creative diverse output with temperature + top-p. Understanding sampling is essential for anyone building LLM applications.</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "14_lora", "file": "main.py", "title": "LoRA (Low-Rank Adaptation)", "tagline": "Freeze the model, inject tiny low-rank matrices, and train only those.", "description": "<p><strong>LoRA</strong> is the most popular parameter-efficient fine-tuning method. Instead of updating all model weights, you freeze them and inject small low-rank matrices (A and B) alongside each target layer.</p><p>The original weight matrix W stays frozen. A new path <code>x → A → B</code> (where A and B have much smaller dimensions) learns the task-specific adaptation. At inference, you can merge <code>W + BA</code> into a single matrix with zero overhead.</p><p>This lab shows LoRA applied to the attention projections. With rank 4, you train ~1% of the original parameters while matching full fine-tuning quality on this task.</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "15_pytorch_quantized", "file": "main.py", "title": "PyTorch Quantized", "tagline": "Compress a trained model from FP32 to INT8.", "description": "<p><strong>INT8 quantization</strong> compresses model weights from 32-bit floats to 8-bit integers — a 4× reduction in model size and memory bandwidth.</p><p>The process: train normally in FP32, then for each weight matrix, compute a scale factor that maps the float range to [-128, 127]. At inference, multiply the int8 weights by the scale to approximate the original floats. The approximation error is small because neural network weights tend to cluster near zero.</p><p>This is how production models run on edge devices, phones, and cost-efficient servers. The lab trains, quantizes, and compares output quality before and after compression.</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "16_text_diffusion", "file": "main.py", "title": "Text Diffusion", "tagline": "Names emerge from pure noise through iterative unmasking.", "description": "<p>Instead of generating left-to-right, <strong>masked diffusion</strong> starts from a sequence of all [MASK] tokens and iteratively reveals them. At each step, the model predicts what's behind every mask, and the most confident predictions are unmasked.</p><p>This is the text equivalent of image diffusion models like Stable Diffusion. The key difference from autoregressive models: the generation order is determined by model confidence, not left-to-right position. The model might unmask the middle of a name before the beginning.</p><p>The lab uses PyTorch for tensors and autograd, but with a fundamentally different training objective (predict masked tokens) and generation process (iterative unmasking with confidence-based remasking).</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "19_speculative_decoding", "file": "main.py", "title": "Speculative Decoding", "tagline": "A small draft model guesses ahead, a larger target model verifies.", "description": "<p><strong>Speculative decoding</strong> uses two models: a small, fast draft model proposes several tokens at once, then the large target model verifies them all in a single forward pass.</p><p>The key insight: verifying N tokens in parallel costs about the same as generating 1 token (because the target model processes them as a batch). If the draft model guesses correctly most of the time, you get N tokens for the cost of ~1. When it guesses wrong, you reject and fall back — the output distribution is mathematically identical to the target model alone.</p><p>This is a lossless speedup: same output quality, fewer forward passes. Used in production by vLLM, TensorRT-LLM, and others.</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "20_tiled_attention", "file": "main.py", "title": "Tiled Attention (FlashAttention)", "tagline": "Three attention implementations producing identical outputs. The difference is memory trips.", "description": "<p><strong>FlashAttention</strong> restructures the attention computation to minimize memory transfers between fast SRAM and slow HBM (main GPU memory).</p><p>Standard attention materializes the full N×N attention matrix in HBM — that's O(N²) memory. FlashAttention processes attention in tiles that fit in SRAM, accumulating results with online softmax (the log-sum-exp trick). The output is mathematically identical, but memory usage drops from O(N²) to O(N).</p><p>This lab implements three versions — naive, standard, and tiled — so you can verify they produce the same output while understanding why the tiled version is faster on real hardware.</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "21_paged_attention", "file": "main.py", "title": "Paged KV Cache (PagedAttention)", "tagline": "Virtual memory paging applied to KV caches.", "description": "<p><strong>PagedAttention</strong> applies the operating system's virtual memory concept to KV caches. Instead of allocating one contiguous block per sequence, KV data is stored in fixed-size pages that can be anywhere in physical memory.</p><p>This solves the fragmentation problem: when sequences have different lengths and lifetimes, contiguous allocation wastes memory on internal fragmentation. Paging eliminates this waste, enabling ~2-4× more concurrent sequences in the same memory budget.</p><p>This is the core innovation behind vLLM. The lab implements it with the same pure-Python scalar autograd as Lab 01, making the memory management logic explicit and inspectable.</p>", "run_command": ["python3", "main.py"]},
    {"id": "17_soft_thinking", "file": "main.py", "title": "Soft Thinking", "tagline": "Pass a probability-weighted blend forward, preserving information.", "description": "<p>Standard autoregressive models collapse the output distribution to a single token via argmax or sampling, then embed that token for the next step. <strong>Soft thinking</strong> instead computes a weighted average of <em>all</em> token embeddings, weighted by the output probabilities.</p><p>This \"concept token\" preserves the model's uncertainty: if the model is 60% sure of 'a' and 40% sure of 'e', the concept token is a blend of both embeddings. No information is discarded. The model can then use this richer signal for subsequent predictions.</p><p>The tradeoff: concept tokens are more expensive to compute (a matrix multiply with the full vocabulary) and the model must learn to interpret blended inputs.</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "18_soft_training", "file": "main.py", "title": "Soft Training", "tagline": "Use concept tokens during training too.", "description": "<p>Soft thinking (Lab 17) uses concept tokens at inference but trains with hard (one-hot) tokens. This creates a <strong>train-test mismatch</strong>: the model never sees blended inputs during training.</p><p>Soft training fixes this with a scheduled curriculum. Early in training, inputs are mostly hard tokens. As training progresses, the model gradually receives more concept tokens as input. By the end, it's fully trained on its own soft outputs.</p><p>The curriculum schedule (linear ramp from 0% to 100% soft inputs) is critical — starting with all-soft inputs from the beginning doesn't work because the model's early predictions are random noise.</p>", "run_command": ["uv", "run", "python", "main.py"]},
    {"id": "22_disaggregated_serving", "file": "main.py", "title": "Disaggregated Serving", "tagline": "Prefill and decode on separate workers.", "description": "<p>LLM inference has two phases with very different compute profiles: <strong>prefill</strong> (process the entire prompt, compute-bound) and <strong>decode</strong> (generate tokens one at a time, memory-bound).</p><p>When both run on the same GPU, long prefills block short decodes, causing latency spikes. Disaggregated serving splits them onto separate workers: prefill workers process prompts and send KV caches to decode workers via a transfer mechanism.</p><p>This eliminates head-of-line blocking and lets you scale each phase independently. The lab simulates this with threads and queues, showing the scheduling and KV cache transfer logic used by production systems like Mooncake and DistServe.</p>", "run_command": ["uv", "run", "python", "main.py"]}
  ]
}
