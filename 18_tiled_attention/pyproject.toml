[project]
name = "18-tiled-attention"
version = "0.1.0"
description = "microGPT with tiled attention (FlashAttention algorithm)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.2",
    "torch>=2.10.0",
]
