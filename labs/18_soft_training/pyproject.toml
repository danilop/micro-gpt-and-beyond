[project]
name = "18-soft-training"
version = "0.1.0"
description = "microGPT with soft training (scheduled soft tokens) in PyTorch"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.2",
    "torch>=2.13.0",
]
