[project]
name = "10-rope"
version = "0.1.0"
description = "microGPT with Rotary Position Embeddings (RoPE)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.2",
    "torch>=2.13.0",
]
