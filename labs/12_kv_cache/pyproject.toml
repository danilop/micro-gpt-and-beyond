[project]
name = "12-kv-cache"
version = "0.1.0"
description = "microGPT with KV cache — the fundamental inference optimization"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.2",
    "torch>=2.13.0",
]
