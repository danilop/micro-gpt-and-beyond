[project]
name = "12-kv-cache"
version = "0.1.0"
description = "microGPT with KV cache — the fundamental inference optimization"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.3",
    "torch>=2.10.0",
]
