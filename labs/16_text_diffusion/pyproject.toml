[project]
name = "16-text-diffusion"
version = "0.1.0"
description = "Masked diffusion language model (MDLM/LLaDA) in PyTorch"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.2",
    "torch>=2.13.0",
]
