[project]
name = "02-numpy-manual-backprop"
version = "0.1.0"
description = "microGPT in NumPy with hand-written backpropagation"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.2",
]
