[project]
name = "02-numpy-autograd"
version = "0.1.0"
description = "microGPT in NumPy with an array-level autograd engine"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.2",
]
