[project]
name = "20-tiled-attention"
version = "0.1.0"
description = "microGPT with tiled attention (FlashAttention algorithm)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.2",
    "torch>=2.13.0",
]
