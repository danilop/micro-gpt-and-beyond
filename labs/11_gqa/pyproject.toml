[project]
name = "11-gqa"
version = "0.1.0"
description = "microGPT with Grouped-Query Attention (MHA vs GQA vs MQA)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.3",
    "torch>=2.10.0",
]
