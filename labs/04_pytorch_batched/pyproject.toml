[project]
name = "04-pytorch-batched"
version = "0.1.0"
description = "microGPT in PyTorch with mini-batch training"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.2",
    "torch>=2.13.0",
]
