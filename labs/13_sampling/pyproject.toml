[project]
name = "13-sampling"
version = "0.1.0"
description = "microGPT sampling strategies — greedy, temperature, top-k, top-p, min-p"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.2",
    "torch>=2.13.0",
]
