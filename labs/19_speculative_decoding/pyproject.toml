[project]
name = "19-speculative-decoding"
version = "0.1.0"
description = "microGPT with speculative decoding in PyTorch"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.2",
    "torch>=2.13.0",
]
