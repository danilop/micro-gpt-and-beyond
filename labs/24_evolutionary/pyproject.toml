[project]
name = "24-evolutionary"
version = "0.1.0"
description = "Evolutionary self-improvement via population-based training"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.3",
    "torch>=2.10.0",
]
